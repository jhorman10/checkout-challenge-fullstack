# OpenSpec changes archive

Completed SDD changes are moved here by the `sdd-archive` phase as
`YYYY-MM-DD-{change-name}/` directories. This is an audit trail — never delete
or modify archived changes.
